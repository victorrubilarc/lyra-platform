Continuamos Lyra WatchLog (`g:\Development\BitacorasInteligentes`). Ejecuta la rutina de arranque de CLAUDE.md ANTES de nada: lee CLAUDE.md y `docs/` (PROGRESS, ARCHITECTURE, DATA_MODEL, SECURITY, DECISIONS, AUTH_FLOW, BACKLOG, USER_GUIDE, FORM_GUIDE). **LECTURA OBLIGADA de este módulo (fuente de verdad, léelas COMPLETAS): `docs/LICENSING_STRATEGY.md` (el porqué — Opción C, 6 capas), `docs/LICENSING.md` (la spec — §5 máquina de estados y §5.1 DTO delgado son EL corazón de esta sesión) y `docs/LICENSING_PROCEDURE.md` (el runbook — §4 renovación ya es REAL desde L4; L6 la vuelve OPERABLE para un admin de planta).** Lee además **COMPLETOS**: `apps/watchlog-api/src/licensing/` (L1/L2/L4: `LicenseService` con linaje rotatorio, `license-runtime.ts` con `toLicenseStatus` y el reason `LINEAGE_MISMATCH`, guards), `packages/contracts/src/licensing/` (`licenseStatusSchema` — el DTO delgado, cimiento del banner), `apps/watchlog-web/src/auth/use-license.ts` (`useLicenseStatus`/`useLicensedModules`, ya consumen el DTO con react-query), el **Topbar/shell de la web** (donde vivirá el banner) y el **motor de notificaciones del Bloque N** (`apps/watchlog-api/src/notifications/`: outbox + worker + resolvers + canales EMAIL/INAPP + preferencias — L6 lo REUSA, no construye mensajería nueva). Revisa `docs/DECISIONS.md` del **2026-07-04** (Opción C) y **2026-07-05** (L1 a–e — en especial el pendiente (iii) de aviso a admins; L2 a–e — decisión c del DTO mínimo privilegio; L3 a–f; **L4 a–f** — la renovación que esta UI hace visible). Revisa tu memoria persistente (MEMORY.md; en especial **licensing-strategy** [L0–L4 ✅; sigue L6/L5/L2b + prueba de fuego EC2], **notifications-engine**, **notif-advanced-requirement**, **deploy-aws**, **product-name**, **challenge-dont-please**, **new-permission-dev-gotcha**, **explain-and-document-userguide**). No des nada por sentado: verifica en el código y en git.

**Antes de empezar:** confirma árbol limpio (`git rev-list --count origin/main..main` = 0; L4 quedó publicado hasta `53d6e23`). **Estado real del que partes:** L0–L4 ✅. El EC2 demo (`lyra.watchlog.itesicws.com`) corre **v0.1.13 (pre-L4)** con `lic_2026_demo_ec2_001` VALIDA (`renewalCounter=0`); el dev local corre L4 (deja `renovacion.lreq` solo). Hoy el estado de licencia solo se ve en logs/auditoría y en el DTO `GET /license/status` — **ningún humano de planta se entera de que debe renovar**: eso es lo que L6 arregla.

**Recordatorios permanentes (challenge-dont-please + gobernanza de CLAUDE.md):** estrategia **DECIDIDA = Opción C** — NO dongle. **La licencia NUNCA secuestra datos** (peor estado = solo lectura + exportación; el banner INFORMA, no bloquea nada nuevo). **Mínimo privilegio se mantiene** (decisión L2c): al front viaja SOLO el DTO delgado — jamás huella/linaje/installationId/customer/licenseId; si crees que el banner necesita un campo más, se agrega al DTO por decisión explícita, no abriendo el snapshot. **Nada que rompa el air-gap** (el aviso es in-app/correo del propio servidor; cero internet exigido). **Permisos configurables, NUNCA roles hardcodeados** (CLAUDE.md §seguridad): el destinatario del aviso se resuelve por permiso/rol configurable como los demás resolvers del Bloque N. Todo texto visible por **i18n** y estilo por **tokens** del design system (nada de hex; el banner debe verse premium en claro y oscuro). Si ves motivo de peso para desviarte, **objétalo con fundamento ANTES de codificar**.

== OBJETIVO DE ESTA SESIÓN (único, cerrable) — Licenciamiento L6: UI de estado + avisos de licencia ==
Que el estado de la licencia sea **VISIBLE y ACCIONABLE** para quien opera la planta: banner de estado, detalle en Configuración y aviso a administradores por el motor de notificaciones (cierra el **pendiente L1(iii)**). Y como cierre operacional, la **prueba de fuego del L4** contra el EC2 demo (pendiente registrado en BACKLOG §2(1)). Alcance EXACTO:

1. **Banner global de estado (web):** componente en el shell (zona Topbar) alimentado por `useLicenseStatus` (el DTO ya existe). Estados y tono: POR_VENCER (aviso, días restantes) · EN_GRACIA (prominente: "licencia vencida, renovar en X días") · SOLO_LECTURA / BLOQUEADA / PENDIENTE_ACTIVACION (persistente: qué significa + qué hacer — spec §5) · LIMITE_EXCEDIDO (aviso a admins). VALIDA no muestra nada. Reason `LINEAGE_MISMATCH` con texto humano propio ("licencia no corresponde a esta instalación — contacta a tu proveedor"). Decide en (a) la **audiencia por estado**: propuesta = estados restringidos y EN_GRACIA los ven TODOS (explican por qué la app no deja escribir); POR_VENCER/LIMITE_EXCEDIDO solo admins (¿gate por permiso configurable? — el DTO es para todo autenticado, el filtro es de UI). Dismissible por sesión donde aplique (los restringidos NO se descartan).

2. **Detalle de licencia en `/configuracion`:** pestaña/sección "Licencia" de solo lectura: estado + motivo humanizado, edición, módulos incluidos (del DTO), vencimiento + días restantes, y las **instrucciones de renovación del runbook** (dónde queda `renovacion.lreq`, a quién enviarlo — texto guía, sin exponer el archivo por API). Decide en (b) el gate: ¿permiso nuevo `license:view` (⇒ gotcha `db:seed` + FLUSHALL) o reusar un permiso existente de configuración? Recomienda con fundamento (el DTO ya es visible para todo autenticado — quizá el detalle no necesita permiso nuevo).

3. **Aviso a administradores (cierra pendiente L1(iii), reusa Bloque N):** al CAMBIAR el estado de la licencia (`LicenseService.refresh` ya detecta la transición y audita), emitir evento(s) al outbox → EMAIL+INAPP vía el motor existente (plantillas default + preferencias por canal como los demás eventos). Decide en (c): catálogo de eventos (¿uno genérico `license.state.changed` o granulares `license.expiring`/`license.restricted`?), **resolución de destinatarios sin hardcodear roles** (propuesta: mismo patrón de los resolvers existentes — p. ej. usuarios con un permiso de administración ya existente, o rol configurable), y anti-spam (dedupe: POR_VENCER re-avisa ¿diario/semanal? — mira cómo lo hace el escalamiento de incidencias).

4. **Marca blanca gobernada por `whiteLabel`:** decide en (d) si entra AHORA o se difiere: mi recomendación es DIFERIR al épico de marca blanca (BACKLOG §2(2)) y dejar solo el **gate latente** (helper/flag que lee `whiteLabel` del payload, hoy sin efecto visible) para no reabrirlo después — L6 se mantiene cerrable en una sesión.

5. **Tests + smoke (obligatorios):** unit de lo nuevo (mapeo estado→presentación; resolver de destinatarios; render/gating del banner si hay tests web) + **smoke en vivo** (`scripts/`, arnés estilo licencia, **puerto NUEVO ≥3404** — 3401/3402/3403 ya están tomados): (i) API con licencia POR_VENCER ⇒ DTO correcto y notificación generada al outbox para el admin (y NO para un usuario sin el criterio); (ii) EN_GRACIA/SOLO_LECTURA ⇒ evento de transición + INAPP en la campanita del admin; (iii) VALIDA ⇒ sin banner/avisos; (iv) regresión: `smoke-licencia.py` 28/28 + `smoke-licencia-modulos.py` 23/23 + `smoke-licencia-emision.py` 20/20 + `smoke-licencia-renovacion.py` 29/29 + los smokes de notificaciones (22/18/18) SIGUEN verdes. El smoke visual del banner lo hago yo (dueño) — déjame el dev con una licencia POR_VENCER de prueba y las instrucciones para volver a la normal.

6. **PRUEBA DE FUEGO L4 (pendiente que NO puede quedar atrás — BACKLOG §2(1)):** al cerrar L6 en verde: cortar **tag `v0.1.14`** (release embebe la pública PROD) → deploy al EC2 (runbook DEPLOYMENT; `update.sh` NO hace `git pull` del clon — hazlo si el compose cambió) → verificar VALIDA → bajar su `renovacion.lreq` → **`pnpm license renew` con la custodia PROD** (la passphrase la ingreso YO — pídemela en el momento o coordina `LYRA_LICENSE_PASSPHRASE` efímera; JAMÁS committear/loguear) → importar → **verificar counter 0→1, nonce rotado, estado VALIDA y banner sano**. Es la primera renovación real del producto: si algo no calza, la licencia demo NO puede quedar rota (plan de reversa: re-importar la licencia vigente ANTES de rotar — ojo: tras rotar, la vieja ya no calza por diseño; ten el orden claro antes de tocar).

== NADA HARDCODEADO / NADA SUELTO ==
Textos por i18n (ES al menos), colores/espaciado por tokens (el banner respeta claro/oscuro y severidad solo semántica). El DTO delgado NO crece salvo decisión explícita en (a)/(b) — y jamás con huella/linaje. Destinatarios de avisos configurables (no "rol admin" en duro). Si agregas permiso: catálogo + seed + FLUSHALL (memoria `new-permission-dev-gotcha`). Migración versionada SOLO si hace falta (no debería: el outbox y las preferencias ya existen).

== LO QUE L6 **NO** HACE (para no invadir sesiones vecinas) ==
- **NO** anti-tamper/bytecode → **L5**. **NO** enforcement de límites numéricos → **L2b** (LIMITE_EXCEDIDO solo se MUESTRA).
- **NO** marca blanca completa (login/PDF/correos con marca del socio) → épico §2(2); aquí a lo sumo el gate latente de (d).
- **NO** portal web de licencias ni heartbeat online; **NO** importar/subir `license.lic` por la UI (la importación sigue siendo archivo del despliegue — un upload por API sería una superficie nueva que hay que pensar aparte; si crees que vale, propónlo como decisión, no lo hagas de pasada).
- **NO** tocar `signLicense`/`verifyLicense`/`evaluateLicense`/`evaluateLineage` de L0 (congelados) ni la lógica de estados — L6 solo PRESENTA y AVISA.

== CONFIRMA ANTES DE CODIFICAR (espera mi visto bueno) ==
(a) **Audiencia y comportamiento del banner por estado** (quién ve qué; qué es dismissible) y si el DTO necesita algún campo nuevo (justifícalo contra mínimo privilegio). (b) **Gate del detalle en /configuracion** (permiso nuevo `license:view` vs reusar existente — recomienda; recuerda el costo seed+FLUSHALL). (c) **Eventos + destinatarios + cadencia del aviso** (genérico vs granulares; resolver sin roles hardcodeados; dedupe/re-aviso). (d) **`whiteLabel`: gate latente ahora, épico después** (mi recomendación) ¿o entra algo más? (e) **Textos**: propón el wording exacto de cada estado del banner (los apruebo antes de implementar — son la cara del producto ante el cliente del socio). (f) **La prueba de fuego del punto 6**: confirma el plan paso a paso (tag → deploy → renew → verificación → reversa) ANTES de tocar el EC2. Si detectas una mejora sobre este alcance, propónmela ahí.

== VERIFICACIÓN Y CIERRE (CLAUDE.md §Gestión de sesiones) ==
1. En verde: `pnpm typecheck && pnpm lint && pnpm build && pnpm test` + smoke nuevo + LAS 4 REGRESIONES de licencia (28/28, 23/23, 20/20, 29/29) + smokes de notificaciones — registra qué se probó y qué NO. **Deja API `:3000/api/health` y web `:5173` CORRIENDO** al cerrar (con la licencia dev VALIDA — si probaste con una POR_VENCER, dime cómo restaurar o restáurala).
2. Actualiza `docs/PROGRESS.md`, `docs/BACKLOG.md` (L6 hecho + pendiente L1(iii) CERRADO + prueba de fuego L4 CERRADA si se hizo; sigue L5/L2b), `docs/LICENSING.md` (§5 avisos = reales) / `LICENSING_PROCEDURE.md` (si cambia lo operacional) como docs VIVOS, `docs/DECISIONS.md` (decisiones a–f). **`docs/USER_GUIDE.md` es OBLIGATORIO en esta sesión** (funcionalidad 100% de cara al usuario): sección Licencia con el banner y los avisos, casos paso a paso (memoria `explain-and-document-userguide`); aprovecha el backfill de 1–2 secciones ✍️ pendientes.
3. Commit descriptivo en rama `feat/licenciamiento-l6` + merge a `main` + **push** (verifica que ni la privada ni passphrases estén en el diff). `git rev-list --count origin/main..main` = 0. El tag `v0.1.14` del punto 6 se corta DESPUÉS del merge en verde.
4. Actualiza la memoria (**licensing-strategy**: L6 hecho + fuego EC2; sigue L5/L2b). **OJO: MEMORY.md sigue sobre el límite de 24.4KB — recorta 2–3 entradas viejas a UNA línea** (el detalle vive en los archivos de tema), no solo agregues.
5. Resumen y dime la frase de cierre con el siguiente módulo (propongo **L5 anti-tamper** para endurecer antes de más ventas, o **L2b límites**, según tu criterio fundamentado).

== ENTORNO Y GOTCHAS (aprendidos hasta L4) ==
Monorepo pnpm workspaces; `@lyra/licensing`, `@lyra/licensing-cli` y `@lyra/contracts` tienen `dist` (si los tocas: `pnpm --filter <pkg> build` o nadie ve el cambio; si tocas contracts, **`db:seed` DESPUÉS del build** o el permiso nuevo no entra). La CLI se invoca `pnpm license <cmd>` pero su **cwd es `packages/licensing-cli`** ⇒ RUTAS ABSOLUTAS en flags. El smoke de renovación (3403) hace **snapshot/restore del linaje dev en la BD** — si escribes uno nuevo que rote linaje, copia ese patrón o dejarás la licencia dev BLOQUEADA. En Python `subprocess` usa `encoding="utf-8", errors="replace"`. Puerto del smoke NUEVO (≥3404), verifica que quede LIBRE entre arranques. PowerShell 5.1: NO `2>&1` con exes nativos, NO heredocs (además el guard del harness puede bloquear here-strings largos: escribe el mensaje de commit con la herramienta Write a `g:\tmp\` y `git commit -F`), commits SIN BOM. Para probar el banner localmente: `pnpm license:dev` acepta variantes (`--expired`, `--modules=a,b`) — mira `tools/gen-dev-license.ts` para generar una POR_VENCER. SSH al EC2 (punto 6): llave `C:\Users\vrubi\Downloads\lyra.pem` copiada a `$env:TEMP` con `icacls /inheritance:r /grant:r "$env:USERNAME:R"` (re-otorgar `:F` antes de borrarla); `update.sh` NO hace `git pull`. Node ≥ 22. **L2/L3/L4 YA están hechos — no los re-hagas**; el EC2 corre v0.1.13 pre-L4 hasta que TÚ despliegues v0.1.14 en el punto 6.
