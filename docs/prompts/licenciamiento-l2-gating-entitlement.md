Continuamos Lyra WatchLog (`g:\Development\BitacorasInteligentes`). Ejecuta la rutina de arranque de CLAUDE.md ANTES de nada: lee CLAUDE.md y `docs/` (PROGRESS, ARCHITECTURE, DATA_MODEL, SECURITY, DECISIONS, AUTH_FLOW, BACKLOG, USER_GUIDE, FORM_GUIDE). **LECTURA OBLIGADA de este módulo (fuente de verdad, léelas COMPLETAS): `docs/LICENSING_STRATEGY.md` (el porqué — Opción C, 6 capas), `docs/LICENSING.md` (la spec técnica — §5 máquina de estados, fila «MÓDULO NO LICENCIADO») y `docs/LICENSING_PROCEDURE.md` (runbook).** Lee además **COMPLETO** el paquete `packages/licensing` (`@lyra/licensing`, L0: `evaluateLicense`/`isModuleLicensed` + tipos `LicensePayload.modules[]`/`edition`) y **todo lo construido en L1** (`apps/watchlog-api/src/licensing/`: `LicenseService` con `isModuleLicensed(key)` YA EXPUESTO Y LATENTE, `getEvaluation()`/`LicenseSnapshot.licensedModules`, el guard global `LicenseEnforcementGuard`, `license-runtime.ts`). Revisa las entradas de `docs/DECISIONS.md` del **2026-07-04** (Opción C + L0) y **2026-07-05** (L1, decisiones a–e; nota que el DTO delgado del estado de licencia se dijo «se define en L2/L6»). Revisa tu memoria persistente (MEMORY.md; en especial **licensing-strategy** [L0✅+L1✅, «SIGUE L2»], **channel-business-model** [edición/módulos = paquete comercial], **stack-decisions**, **product-name**, **challenge-dont-please**, **decide-well-align-to-leaders**, **new-permission-dev-gotcha**, **inicio-launchpad** [el registro `SIDEBAR_ROUTES` gatea por permiso — aquí se le suma el gate por módulo], **theme-system**). No des nada por sentado: verifica en el código y en git.

**Antes de empezar:** confirma árbol limpio (`git rev-list --count origin/main..main` debe ser 0; L1 quedó publicado en `main` hasta el merge `b5c7c51`). Si hubiera algo sin publicar, resuélvelo antes.

**Recordatorios permanentes (challenge-dont-please + gobernanza de licenciamiento de CLAUDE.md):** el entitlement gobierna la **INSTALACIÓN** (qué módulos encendió la licencia = `modules[]`/`edition`) y es un **eje DISTINTO del RBAC/ABAC**, que gobierna al **USUARIO**. Ambos deben pasar: un módulo se usa si está **licenciado Y el usuario tiene permiso**. La regla de oro sigue mandando: **la licencia NUNCA secuestra datos** — un módulo no licenciado (p. ej. tras un downgrade de edición) queda en **SOLO LECTURA + exportación** de sus datos, JAMÁS bloqueo total ni borrado. **La verificación es DISTRIBUIDA:** reusa el `LicenseService` de L1 (no inventes un segundo origen de verdad del estado). **Nada que rompa el air-gap.** Si ves motivo de peso para desviarte de la spec/L0/L1, **objétalo con fundamento ANTES de codificar**.

== OBJETIVO DE ESTA SESIÓN (único, cerrable) — Licenciamiento L2: gating de módulos por entitlement en API + web ==
Activar los gates LATENTES que L1 dejó preparados: que un módulo **no incluido en la licencia** no se pueda operar ni aparezca, respetando «nunca secuestrar datos». Alcance EXACTO:

1. **Catálogo canónico de claves de módulo** (decide dónde vive en (a)): una única fuente de verdad de las claves de módulo licenciables del producto (hoy `modules[]` es dato libre del payload; la licencia DEV de L1 ya usa `core/structure/templates/logbook/schedules/incidents/exceptions/work-orders/shift-handover/notifications/themes/ai/dashboards`). Formaliza ese catálogo como constante compartida (probablemente en `@lyra/licensing` o `@lyra/contracts`) para que backend y front hablen el mismo idioma y agregar un módulo al producto registre su clave (regla CLAUDE.md: «todo módulo nuevo nace entitlement-aware»).

2. **Gate de entitlement en el BACKEND** (decide comportamiento en (b)): un `@RequireModule('<clave>')` + guard que consulta `LicenseService.isModuleLicensed()` (el snapshot cacheado de L1, costo ~0). Semántica coherente con «nunca secuestra datos»: módulo no licenciado ⇒ **se bloquean las MUTACIONES** de ese módulo (POST/PUT/PATCH/DELETE) con `403 { code: "MODULE_NOT_LICENSED", module }`; **la lectura y exportación de ese módulo SIGUEN disponibles** (GET). Es el mismo principio del guard global de L1, pero **por módulo** y aplicable **aun estando la licencia VALIDA** (un downgrade de edición no vencida). Aplica el decorator a los controladores de cada módulo licenciable; el `core` no se gatea (siempre encendido). Defensa en profundidad: el gate por módulo NO reemplaza el RBAC — ambos guards corren.

3. **DTO delgado de estado de licencia + endpoint** (decide forma en (c)): expón lo MÍNIMO para que la web decida qué mostrar — `{ status, edition, modules[], expiresAt?, daysToExpiry? }`, **mapeado desde `LicenseEvaluation`/`LicenseSnapshot`, NUNCA el payload completo** (sin huella, sin linaje, sin installationId — mínimo privilegio, DECISIONS 2026-07-05). Contrato en `@lyra/contracts` (OJO: es cambio de contracts ⇒ reconstruir `dist` o nadie lo ve). Endpoint tipo `GET /license/status` (autenticado; decide si requiere permiso o basta estar logueado). Este DTO es también el cimiento del banner de L6.

4. **Gate de entitlement en el FRONTEND (ocultar/deshabilitar):** el front consume el DTO y **oculta los módulos no licenciados** igual que hoy oculta por permiso (el registro `SIDEBAR_ROUTES` ya gatea por `can(...)`; súmale el gate por módulo licenciado). La combinación visible = **módulo licenciado ∧ permiso del usuario**. El backend sigue siendo la fuente de verdad (el front solo oculta; el 403 del gate del backend es el candado real). Cuida el Inicio/cockpit (los tiles ya se gatean por permiso — añade el de módulo sin romper el «tile en 0 = calmo»).

5. **Interacción con la máquina de estados de L1:** documenta y respeta la precedencia — SOLO_LECTURA/BLOQUEADA/PENDIENTE_ACTIVACION (global, L1) ya bloquean TODA mutación; el gate por módulo es un filtro ADICIONAL por edición cuando la licencia está operativa. No dupliques lógica: reusa el estado del `LicenseService`.

6. **Tests + smoke (obligatorios — es lógica de negocio de pago):** unit del guard de módulo (licenciado ⇒ pasa; no licenciado ⇒ mutación 403, lectura/export OK; core siempre; entitlement × RBAC = ambos exigidos). Unit del mapeo DTO (no filtra huella/linaje). **Smoke en vivo** (`scripts/`, reusa el arnés de `smoke-licencia.py`): arrancar la API con una licencia `modules:[core,incidents]` → incidents opera, un módulo FUERA de la lista (p. ej. `work-orders`) da 403 en mutación pero 200 en lectura/export; `GET /license/status` refleja exactamente esos módulos y NO expone campos sensibles. Los 276 tests de la API + web + CI siguen EN VERDE.

== NADA HARDCODEADO / NADA SUELTO ==
Las claves de módulo salen del catálogo único (punto 1), no strings sueltos por controlador. El DTO se deriva del `LicenseSnapshot`, no se re-arma a mano. El front no decide entitlement por su cuenta: consume el DTO del backend. Reusa `LicenseService`/`isModuleLicensed` de L1 y `@lyra/licensing` — cero re-implementación de la evaluación.

== LO QUE L2 **NO** HACE (para no invadir sesiones vecinas) ==
- **NO** CLI de emisión ni custodia de clave privada ni pública de PROD embebida → **L3** (la imagen sigue NO vendible hasta L3/L5; no lo cambies aquí).
- **NO** challenge-response por USB, renovación ni rotación de linaje → **L4**.
- **NO** bytecode/ofuscación/anti-tamper → **L5**.
- **NO** el banner/pantalla completa de estado de licencia ni la marca blanca gated por `whiteLabel` → **L6** (L2 deja el DTO mínimo listo, pero la UI rica de avisos POR_VENCER/EN_GRACIA/SOLO_LECTURA es L6).
- **NO** enforcement de LÍMITES numéricos (`maxNodes`/`maxNamedUsers` al crear por encima del tope) salvo que decidamos incluirlo en (d) — es primo del gating pero distinto; hoy L1 solo lo REGISTRA (LIMITE_EXCEDIDO no restringe).

== CONFIRMA ANTES DE CODIFICAR (espera mi visto bueno) ==
(a) **Dónde vive el catálogo canónico de claves de módulo** (`@lyra/licensing` vs `@lyra/contracts`) y su granularidad (por módulo del sidebar, o más fino). (b) **Comportamiento del módulo no licenciado**: mutación 403 + lectura/export permitidas (mi recomendación, coherente con «nunca secuestra datos») vs ocultar/403 total — decídelo explícito. (c) **Forma y gate del DTO/endpoint `GET /license/status`** (campos exactos, si requiere permiso o basta autenticación). (d) **Si L2 también hace enforcement de LÍMITES numéricos** (crear por encima de `maxNodes`/`maxNamedUsers`) o se deja para después. (e) **Migración/permiso**: ¿hace falta un permiso nuevo (recuerda el gotcha `db:seed` + Redis `FLUSHALL`) o el endpoint de estado va sin permiso? (probablemente sin migración y sin permiso, pero confírmalo). Si detectas una mejora sobre la spec, propónmela ahí.

== VERIFICACIÓN Y CIERRE (CLAUDE.md §Gestión de sesiones) ==
1. En verde: `pnpm typecheck && pnpm lint && pnpm build && pnpm test` + smoke en vivo del gate de módulo (registra qué se probó y qué NO). **Deja API `:3000/api/health` y web `:5173` CORRIENDO** al cerrar.
2. Actualiza `docs/PROGRESS.md`, `docs/BACKLOG.md` (L2 hecho en §2(1), **L3 próxima** — o L4 según lo acordado), `docs/LICENSING.md` (§5 fila MÓDULO NO LICENCIADO reflejando lo construido; el catálogo de módulos), `docs/DECISIONS.md` (decisiones a–e), `@lyra/contracts` si sumaste el DTO, y `.env.example` si aplica. **USER_GUIDE**: si el usuario ve módulos aparecer/desaparecer o un mensaje «módulo no incluido en tu licencia», redáctalo (§ Licencia de la instalación ya existe — amplíala).
3. Commit descriptivo en rama `feat/licenciamiento-l2` + merge a `main` + **push** (incluye `.claude/settings.json` si cambió). Verifica `git rev-list --count origin/main..main` = 0.
4. Actualiza la memoria (**licensing-strategy**: L2 hecho con el gate de módulo real + DTO delgado; deja L3/L4 como lo que sigue).
5. Resumen y dime la frase de cierre con el siguiente módulo (L3 CLI de emisión / custodia, o L4 challenge-response/linaje, según lo que quede).

== ENTORNO Y GOTCHAS ==
Monorepo pnpm workspaces. `@lyra/licensing` **ya NO es source-only**: tiene `dist` (espejo de `@lyra/contracts`; la API CJS no importa TS fuente — DECISIONS 2026-07-05); si lo tocas, `pnpm --filter @lyra/licensing build`. **`@lyra/contracts` se consume desde `dist`: si agregas el DTO, reconstruye o el backend/web no lo ven.** API NestJS `:3000` prefijo `/api`, web `:5173`; tests = vitest. El front gatea el sidebar por el registro `SIDEBAR_ROUTES` (fuente única) + `can(...)`; el gate por módulo se suma ahí, no en cada pantalla. PowerShell 5.1: NO `2>&1` con exes nativos; NO heredoc bash — commits largos con `git commit -F archivo`. **Permiso nuevo ⇒ `db:seed` + Redis `FLUSHALL` o el admin demo da 403** (evalúa en (e) si de verdad lo necesitas). El dev server corriendo puede bloquear el DLL de Prisma (`EPERM` en `prisma generate`: benigno si el cliente ya está generado, o detén el dev). Gotcha del smoke de licencia (de L1): verifica que el puerto quede LIBRE entre arranques de la API. Node ≥ 22.
