# Prompt de sesión — OOBE S3 · Branding real (identidad aplicada + logo + login runtime)

> Registrado 2026-07-06 al cierre de la sesión OOBE S1+S2. Copiar/pegar como prompt de la sesión nueva.

---

Continuamos Lyra WatchLog (g:\Development\BitacorasInteligentes). Ejecuta la rutina de arranque de CLAUDE.md ANTES de nada: lee CLAUDE.md y docs/ (PROGRESS, ARCHITECTURE, DATA_MODEL, SECURITY, DECISIONS, AUTH_FLOW, BACKLOG, USER_GUIDE). LECTURA OBLIGADA permanente: docs/LICENSING_STRATEGY.md + docs/LICENSING.md (en especial la decisión L6d: gate LATENTE `whiteLabel` del payload verificado — esta sesión lo CABLEA por primera vez; y la regla "jamás ITESICWS en textos de cliente"). Lee COMPLETO el ítem BACKLOG §2 épico distribución → (5) S3 (la spec registrada: aplicar lo que el wizard ya PERSISTE) y su hermano §2(2) "Modo marca blanca COMPLETO" (S3 es su primera rebanada — NO lo hagas entero). Lee además: docs/prompts/setup-wizard-oobe-s1-s2.md (el contexto de dónde viene esto), apps/watchlog-api/src/setup/setup.service.ts (finalize: qué guarda en SystemSettings — companyDisplayName/defaultTimezone/defaultLocale/defaultThemeMode YA persisten, hoy nadie los LEE), apps/watchlog-web/src/branding.ts + features/auth/AuthLayout.tsx (el co-branding del login hoy es BUILD-TIME por VITE_LICENSEE_NAME/VITE_LICENSEE_LOGO — el problema que esta sesión mata: marca por rebuild no sirve para canal), apps/watchlog-web/src/shell/ (Topbar + theme-store: dónde entra el fallback defaultThemeMode), apps/watchlog-api/src/storage/ (StorageService MinIO: subida proxied + presigned GET — OJO: el login necesita el logo SIN sesión), src/settings/ (SystemSettings service/controller existente) y src/licensing/license.service.ts (isWhiteLabelEnabled(), L6d). Revisa tu memoria persistente (MEMORY.md; en especial setup-wizard-oobe [S1+S2 ✅ + gotchas], theme-system, channel-business-model, challenge-dont-please, decide-well-align-to-leaders).

Antes de empezar: confirma árbol limpio (git rev-list --count origin/main..main = 0). Estado real del que partes: OOBE S1+S2 COMPLETOS y publicados (asistente /setup operativo, seed split hecho, RUN_SEED/BOOTSTRAP_ADMIN_* eliminados); SystemSettings ya tiene companyDisplayName/defaultTimezone/defaultLocale/defaultThemeMode (columnas pobladas por el wizard, SIN consumidor); tag de producto DIFERIDO a propósito (v0.1.16 pendiente desde L2b; probablemente esta sesión SÍ amerita tag al cerrar el flujo de entrega — recomiéndame). El EC2 demo corre v0.1.15 y NO se toca. Dev local: API :3000 + web :5173 corriendo, licencia VALIDA holgada, BD dev setupCompleted=true.

Recordatorios permanentes (challenge-dont-please + gobernanza): licencia = Opción C, verificación DISTRIBUIDA, jamás secuestrar datos, nada que rompa el air-gap. Todo módulo nuevo nace entitlement-aware — aquí es LITERAL: el gate `whiteLabel` del payload gobierna hasta dónde se re-marca (L6d se cablea EN esta sesión). Si ves motivo de peso para desviarte, objétalo con fundamento ANTES de codificar.

== CONTEXTO / POR QUÉ ==
El wizard de primer arranque ya captura la identidad de la empresa… y nadie la muestra. Peor: el co-branding del login es BUILD-TIME (`VITE_LICENSEE_*` en el .env del build) — para el modelo de canal (memoria channel-business-model: socio mayorista marca blanca, N clientes con la MISMA imagen) la marca DEBE ser runtime, por instalación, sin rebuild. Esta sesión hace que lo configurado se VEA y sienta las bases del épico marca blanca §2(2).

== OBJETIVO DE ESTA SESIÓN (único, cerrable) — OOBE S3 · branding runtime ==
Que el nombre y el logo de la empresa configurados en la instalación se apliquen EN VIVO (sin rebuild) a la web: Topbar, título del documento y pantalla de login (co-branding), con captura de logo desde /configuracion y desde el wizard; y que `defaultThemeMode` por fin gobierne el fallback de tema. Alcance EXACTO:

S3a · Backend:
- Endpoint PÚBLICO de branding (pre-auth: el login lo necesita SIN sesión): `GET /api/branding` (o similar) que devuelva SOLO lo presentable — nombre visible, si hay logo (+ cómo obtenerlo), defaultThemeMode, y `whiteLabel` efectivo (booleano del payload verificado vía isWhiteLabelEnabled()). Mínimo privilegio estricto: NADA más se filtra a anónimos (ni edición, ni licencia, ni installationId). Whitelist L1 si aplica (es GET ⇒ pasa solo; verifica).
- Logo de la instalación: subida desde /configuracion (permiso settings:manage o el que corresponda — justifica) + persistencia (decide (a): MinIO vía StorageService vs bytes en Postgres — el logo es chico y el login lo pide SIN auth ni presigned; recomienda UNA con fundamento) + servido público cacheable. Validaciones: tipo (png/svg/jpg — OJO SVG = XSS si se sirve inline, decide y justifica), tamaño (≤ 512KB?), y se ALMACENA re-dimensionado o tal cual (sin sobre-ingeniería). Auditado.
- El wizard /setup gana la captura de logo (paso Apariencia o Identidad — decide) reusando el MISMO endpoint/servicio (token-gated en modo setup). Aditivo: el wizard sigue funcionando sin logo.
- Gate whiteLabel (L6d, PRIMERA vez que hace algo): decide (b) la matriz exacta — propuesta: SIN whiteLabel ⇒ co-branding (marca Lyra + "Licenciado para {empresa}" con su logo, como hoy pero runtime); CON whiteLabel:true ⇒ la marca del cliente/socio toma el lugar dominante en login/Topbar (Lyra queda en "Operado con Lyra WatchLog" discreto). Los CORREOS y el ACTA PDF quedan FUERA (épico §2(2); el acta es Part 11 — no la toques).

S3b · Web:
- `useBranding()` (query pública) + aplicar: Topbar (nombre junto al selector/identidad), `document.title` ("{empresa} · Lyra WatchLog" o según whiteLabel), y AuthLayout/login consumiendo el branding RUNTIME (retira `VITE_LICENSEE_*` — decide (c) si queda como fallback de transición o muere; recomienda).
- Fallback `defaultThemeMode`: theme-store respeta el default de la instalación cuando el usuario no tiene preferencia local (localStorage vacío) — sin pisar preferencias existentes.
- /configuracion (tab que corresponda — ¿Apariencia? ¿nueva "Identidad"?): editar nombre visible + subir/quitar logo + elegir defaultThemeMode/zona horaria/idioma (los campos del wizard, editables post-setup). Gate por permiso; decide (d) si amerita permiso nuevo (OJO gotcha: db:seed + FLUSHALL) o reusa settings:manage.

== INVESTIGA / DECIDE ANTES DE CODIFICAR (espera mi visto bueno) ==
(a) Dónde vive el logo (MinIO vs bytea en Postgres vs archivo en volumen) considerando: acceso SIN auth, backup (pg_dump lo respalda si va en BD), tamaño chico, air-gap. (b) Matriz whiteLabel on/off exacta (qué ve el usuario final en cada modo, login y Topbar). (c) Destino de VITE_LICENSEE_* (retirar vs fallback). (d) Permiso del branding. (e) ¿Tag al cierre? (recomiéndame: probablemente SÍ — v0.1.16 con L2b+OOBE completo+branding = primera imagen VENDIBLE con entrega digna). (f) Smoke nuevo puerto ≥3408 (3401–3407 TOMADOS): branding público devuelve solo lo presentable · subir logo válido/ inválido (tipo/tamaño) · nombre aplicado · whiteLabel on/off (gen-dev-license ¿soporta --white-label? verifica; si no, agrégalo al tooling dev) · wizard con logo · setup completado no expone nada extra. Si detectas una mejora sobre este alcance, propónmela ahí.

== LO QUE ESTA SESIÓN NO HACE ==
NO correos ni acta PDF re-marcados (épico §2(2); el acta es Part 11). NO tocar linaje/licencia del EC2 ni re-abrir L0–L6. NO temas nuevos (EST-TEMAS 2B/2C siguen en su cola). NO login personalizable más allá del co-branding/whiteLabel definido en (b). NO portal de licencias.

== VERIFICACIÓN Y CIERRE (CLAUDE.md §Gestión de sesiones) ==
En verde: typecheck/lint/build/test + smoke nuevo (≥3408) + smoke-setup 25/25 (tocas el wizard) + regresiones: licencia 28 · modulos 24 · integridad 35 (si tocas nombres del módulo licencia: whiteLabel) + tema-paletas/temas-plantillas (tocas theme-store) + 1 de estructura — registra qué se probó y qué NO (smoke VISUAL mío). Deja API :3000 y web :5173 CORRIENDO.
Actualiza docs/PROGRESS.md, docs/BACKLOG.md (§2(5) CERRADO si S3 queda completo; §2(2) marca lo que S3 ya adelantó), docs/DECISIONS.md (a–f), docs/LICENSING.md (L6d cableado: qué gobierna whiteLabel hoy), docs/SECURITY.md (endpoint público de branding + validación de subida), docs/ARCHITECTURE.md si aplica, docs/USER_GUIDE.md (sección "Identidad y logo de la empresa" + actualizar §20 primer arranque; backfill 1–2 ✍️), docs/SALES_GUIDE.md (marca blanca runtime = argumento de canal).
Commit descriptivo en rama feat/oobe-s3-branding + merge a main + push. rev-list = 0. Tag según decisión (e). Actualiza la memoria (setup-wizard-oobe: S3 cerrado; theme-system si tocaste el fallback). Resumen y frase de cierre proponiendo el siguiente foco (QA del dueño / reversa GxP / épico §2(2) restante o §2(4) cadena de suministro).

== ENTORNO Y GOTCHAS ==
Monorepo pnpm; si tocas @lyra/contracts: `pnpm --filter build` ANTES del typecheck de la API. Permiso nuevo ⇒ db:seed + Redis FLUSHALL (gotcha admin demo 403). Migración = SIEMPRE `prisma migrate dev` (aditiva) con `dotenv -e ../../.env --`; si `prisma generate` da EPERM, mata el PID de :3000 primero (Get-NetTCPConnection -LocalPort 3000) y relánzalo al final. Zod en contracts: claves `.optional()` RECHAZAN null (omitir la clave; jamás .default() — rompe TS de la web). Puertos smoke 3401–3407 TOMADOS ⇒ ≥3408; si el smoke necesita instalación virgen usa el patrón BD efímera de smoke-setup.py (prisma db execute con DROP/CREATE en llamadas SEPARADAS). Python subprocess encoding="utf-8" errors="replace". PowerShell 5.1: NO &&, NO heredocs (commit → Write a g:\tmp + git commit -F), commits SIN BOM. El dev :3000 corre node dist/main SIN watch (recompilaste ⇒ reinícialo). Node ≥ 22. El EC2 demo NO se toca (el tag nuevo, si lo hay, SÍ lo desplegará solo — eso es esperado y deseado; verifica el health después).
