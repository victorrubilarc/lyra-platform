Continuamos Lyra WatchLog (`g:\Development\BitacorasInteligentes`). Ejecuta la rutina de arranque de CLAUDE.md ANTES de nada: lee CLAUDE.md y `docs/` (PROGRESS, ARCHITECTURE, DATA_MODEL, SECURITY, DECISIONS, AUTH_FLOW, BACKLOG, USER_GUIDE, FORM_GUIDE). **LECTURA OBLIGADA de este módulo (fuente de verdad, léelas COMPLETAS): `docs/LICENSING_STRATEGY.md` (§5 anti-tamper es EL corazón de esta sesión — y su honestidad: es CAPA DE REFUERZO, nunca el candado), `docs/LICENSING.md` (§7 capas de disuasión) y `docs/LICENSING_PROCEDURE.md` (§6 — dónde y cuándo se ofusca: en el CI, UNA vez por versión, JAMÁS por cliente).** Lee además **COMPLETOS**: `docker/Dockerfile.api` y `docker/Dockerfile.web` (cómo se construye HOY la imagen: ¿tsc plano? ¿minifica algo?), `.github/workflows/release.yml` (el pipeline real: embed de la pública PROD ya corre ahí — L5 se cuelga del MISMO punto), `packages/licensing/` completo (el módulo crítico a proteger: cero deps, `node:crypto`, dist CJS) y `apps/watchlog-api/src/licensing/` (los consumidores: `license-public-key.ts` constante compilada, guards, `LicenseService`, y el chequeo distribuido del worker). Revisa `docs/DECISIONS.md` del **2026-07-04** (Opción C — capa 3 = anti-tamper) al **2026-07-06** (L6 + prueba de fuego: la denegación del ledger y la re-emisión — contexto de dónde estamos). Revisa tu memoria persistente (MEMORY.md; en especial **licensing-strategy** [L0–L4+L6 ✅ + fuego ✅; sigue L5/L2b], **deploy-aws**, **ci-pipeline** [cómo se reparó ci.yml y cómo inspeccionar runs sin `gh`], **product-name**, **challenge-dont-please**). No des nada por sentado: verifica en el código y en git.

**Antes de empezar:** confirma árbol limpio (`git rev-list --count origin/main..main` = 0; la sesión L6 quedó publicada hasta `9e25358`, tag `v0.1.14`). **Estado real del que partes:** L0–L4 + L6 ✅ y la **prueba de fuego L4 EJECUTADA**: el EC2 demo (`lyra.watchlog.itesicws.com`) corre **v0.1.14** con `lic_2026_demo_ec2_001` **renovada** (counter=1, linaje rotado, vence 2027-07-06). **OJO dev local:** quedó con una licencia **POR_VENCER** a propósito (smoke visual del banner L6 del dueño) — si el dueño ya hizo su smoke, restaura la normal (`pnpm license:dev` + reiniciar el api dev) ANTES de trabajar; si no, pregúntale. Hoy la imagen publicada lleva la pública PROD embebida pero **todo el JS de la API viaja LEGIBLE** (dist de tsc sin minificar): un socio con un dev puede leer `license.service.js` y buscar qué parchar. Eso es lo que L5 encarece.

**Recordatorios permanentes (challenge-dont-please + gobernanza de CLAUDE.md):** estrategia **DECIDIDA = Opción C** — NO dongle. **Honestidad de la capa 3 (STRATEGY §5, investigación citada):** el bytecode V8 se puede desensamblar y Node SEA solo es DÉBIL — el anti-tamper **encarece y retrasa**, no vuelve imposible; NO lo vendas ni lo documentes como bóveda. **La verificación es DISTRIBUIDA** (ya lo es: guard + worker + linaje) — L5 no la centraliza, la ENDURECE. **La licencia NUNCA secuestra datos** (nada de L5 puede convertir un fallo de integridad en algo destructivo: el peor estado sigue siendo solo lectura + exportación). **Nada que rompa el air-gap** ni dependencias SaaS. **dev y CI (ci.yml) siguen SIN protección** — el horneado ocurre SOLO en el build de release (mismo patrón que el embed de la pública, decisión L3c); el DX local no se toca. Si ves motivo de peso para desviarte, **objétalo con fundamento ANTES de codificar**.

== OBJETIVO DE ESTA SESIÓN (único, cerrable) — Licenciamiento L5: anti-tamper del módulo crítico en el build de release ==
Que el **módulo de licenciamiento deje de ser texto editable** en la imagen distribuida y que **parchar el chequeo cueste caro**: bundle+minificación del código de la API, horneado del módulo crítico (`@lyra/licensing` + su cableo) y auto-verificación de integridad — todo en el CI de release, una vez por versión (PROCEDURE §6). Alcance EXACTO:

1. **INVESTIGA ANTES DE PROPONER (esto es lo primero y condiciona todo):** la técnica concreta para NestJS 11 + Prisma en Node 22. Campo minado conocido que debes resolver con evidencia (no de memoria): (i) **esbuild NO soporta `emitDecoratorMetadata`** y Nest depende de reflect-metadata ⇒ ¿bundle con esbuild+plugin swc, swc directo, o tsc→minify sobre el dist? (ii) **Prisma client** (engines nativos, `@prisma/client` generado) y dependencias con assets (pdfmake/fuentes, minio, bcrypt/argon2 nativos) suelen quedar FUERA del bundle (`external`) — define qué se bundlea y qué no; (iii) **bytenode/bytecode V8**: ata el artefacto a la versión EXACTA de V8/Node de la imagen (fija la base image), exige un loader en runtime y se lleva mal con ESM — evalúa si el juego lo vale para `@lyra/licensing` + `licensing/` de la API, o si un **bundle único minificado con nombres destruidos + strings de licencia ofuscadas** ya cumple el objetivo de la capa 3 con MUCHO menos riesgo operacional. Trae la comparación con pros/contras REALES verificados en el repo (no teoría) a la decisión (a).
2. **Horneado en release.yml / Dockerfile.api:** el paso corre junto al embed de la pública (release SOLO); la imagen resultante NO contiene el fuente legible del módulo de licencia (y, si la técnica elegida lo da barato, del resto de la API). dev/ci.yml intactos y verdes sin nada de esto.
3. **Auto-verificación de INTEGRIDAD (capa §7.4, distribuida):** el runtime comprueba que sus piezas críticas no fueron parchadas — p. ej. hash de los artefactos críticos calculado EN EL BUILD y verificado en ≥2 puntos independientes ya existentes (arranque de `LicenseService` y el chequeo del worker; NO crees un verificador central único). Fallo de integridad ⇒ estado RESTRINGIDO presentable (reusa la máquina de estados / un reason nuevo tipo `INTEGRITY_MISMATCH` en el runtime L1 — el enum L0 NO se toca), jamás crash ni nada destructivo, y se AUDITA. Decide en (c) el alcance exacto (qué archivos se sellan, dónde vive el hash, y el comportamiento en dev donde no hay sello).
4. **Verificación de que QUEDÓ protegido (no confíes en el build verde):** un smoke que construya la imagen de release localmente (o inspeccione la del CI) y afirme: (i) el texto plano de `license.service` / `verifyLicense` NO es localizable (grep de strings distintivas), (ii) la app arranca y la licencia evalúa VALIDA igual que antes (la protección no puede romper el runtime), (iii) adulterar un artefacto sellado ⇒ estado restringido con el reason nuevo. Puerto NUEVO ≥3405 si levanta API (3401–3404 tomados).
5. **Tests + regresiones (obligatorios):** unit de lo nuevo + `pnpm typecheck && pnpm lint && pnpm build && pnpm test` + las 5 regresiones de licencia (`smoke-licencia.py` 28 · `-modulos` 23 · `-emision` 20 · `-renovacion` 29 · `-avisos` 25) + notificaciones (18/22/18) — TODAS deben seguir verdes con dev SIN hornear (el carril dev no cambia).
6. **Cierre operacional:** si todo está en verde y la imagen protegida arranca VALIDA en el smoke local, corta **tag `v0.1.15`** (release automático → EC2) y verifica en el demo: health 200, `estado=VALIDA` (counter=1 intacto — NO toques la licencia ni el linaje del EC2 en esta sesión) y que el JS de la imagen desplegada ya no sea legible. Si el release falla, la reversa es re-taggear la imagen anterior (el EC2 sigue con v0.1.14 funcionando — nada urge).

== NADA HARDCODEADO / NADA SUELTO ==
El sello de integridad se GENERA en el build (nunca committeado a mano); rutas/nombres de artefactos sellados en UNA constante clara, no strings regados. Ninguna clave nueva; la privada de emisión sigue JAMÁS en repo/imagen/.env. Si algo del pipeline cambia rutas del compose/deploy, recuerda que `update.sh` NO hace `git pull` (runbook). Documentar la honestidad: en LICENSING.md §7 el estado pasa de "propuesto" a "construido", manteniendo el lenguaje de REFUERZO (no bóveda).

== LO QUE L5 **NO** HACE (para no invadir sesiones vecinas) ==
- **NO** enforcement de límites numéricos → **L2b**. **NO** marca blanca → épico §2(2).
- **NO** firma de imágenes/cosign/SBOM/digest-pinning → épico **§2(4) cadena de suministro** (es otra capa: proteger el CANAL de distribución, no el contenido del binario). Si al tocar release.yml ves quick-wins de (4), anótalos en BACKLOG, no los hagas.
- **NO** tocar `signLicense`/`verifyLicense`/`evaluateLicense`/`evaluateLineage` (L0 congelado) ni la máquina de estados — L5 protege el ENVOLTORIO, no cambia la lógica.
- **NO** ofuscar la web (el frontend no contiene secretos de licencia; su minificación de Vite ya es la que es).

== CONFIRMA ANTES DE CODIFICAR (espera mi visto bueno) ==
(a) **Técnica elegida** para módulo crítico y API (bytecode V8 vs bundle+minify agresivo vs híbrido), con la evidencia de la investigación del punto 1 y el costo operacional honesto (versión de Node atada, debugging de prod, tamaño de imagen). Recomienda UNA. (b) **Alcance del bundle** (¿toda la API o solo el árbol de licencia?) y la lista de `external`. (c) **Diseño de la integridad**: qué se sella, dónde vive el hash del build, los ≥2 puntos de verificación, el reason nuevo y el comportamiento en dev/CI sin sello. (d) **Nombre y semántica del estado/reason** ante integridad rota (propuesta: BLOQUEADA + `INTEGRITY_MISMATCH`, restringido jamás destructivo — el banner L6 lo mostrará con el texto genérico de licencia inválida ¿o merece texto propio? decide). (e) **Plan del smoke de imagen** (cómo construyes/inspeccionas la imagen en Windows local: docker build del Dockerfile.api con el paso de horneado activado a mano). (f) **El corte de v0.1.15 del punto 6** (¿en esta sesión o diferido?). Si detectas una mejora sobre este alcance, propónmela ahí.

== VERIFICACIÓN Y CIERRE (CLAUDE.md §Gestión de sesiones) ==
1. En verde: typecheck/lint/build/test + smoke nuevo de imagen + LAS 5 regresiones de licencia + smokes de notificaciones — registra qué se probó y qué NO. **Deja API `:3000/api/health` y web `:5173` CORRIENDO** al cerrar, con la licencia dev VALIDA.
2. Actualiza `docs/PROGRESS.md`, `docs/BACKLOG.md` (L5 hecho; queda L2b + épico §2(2)/(4)), **`docs/LICENSING.md` §7 y `LICENSING_PROCEDURE.md` §6** (de propuesto → construido, con la honestidad intacta), `docs/DECISIONS.md` (decisiones a–f), `docs/SECURITY.md` si el pipeline de build cambió. `docs/USER_GUIDE.md` probablemente NO aplica (L5 no es de cara al usuario) — pero aprovecha el backfill de 1–2 secciones ✍️ pendientes.
3. Commit descriptivo en rama `feat/licenciamiento-l5` + merge a `main` + **push** (verifica que ningún sello/clave quede committeado a mano). `git rev-list --count origin/main..main` = 0. El tag `v0.1.15` según decisión (f).
4. Actualiza la memoria (**licensing-strategy**: L5 hecho; sigue L2b). MEMORY.md está OK de tamaño (9.7KB) — solo agrega la línea.
5. Resumen y dime la frase de cierre con el siguiente módulo (propongo **L2b límites numéricos** para cerrar el plan L0–L6 completo, o retomar el roadmap de producto — QA del dueño / reversa GxP — según tu criterio fundamentado).

== ENTORNO Y GOTCHAS (aprendidos hasta L6) ==
Monorepo pnpm workspaces; `@lyra/licensing`, `@lyra/licensing-cli` y `@lyra/contracts` tienen `dist` (si los tocas: `pnpm --filter <pkg> build`; si tocas contracts, `db:seed` DESPUÉS del build). Puertos de smoke 3401–3404 TOMADOS ⇒ usa ≥3405 y verifica que quede libre entre arranques. En Python `subprocess` usa `encoding="utf-8", errors="replace"`. PowerShell 5.1: NO `2>&1` con exes nativos, NO heredocs (mensaje de commit → Write a `g:\tmp\` + `git commit -F`), commits SIN BOM. El dev server de :3000 corre `node dist/main` SIN watch: si recompilas la API, reinícialo tú (busca el PID con `Get-NetTCPConnection -LocalPort 3000`). La BD dev es COMPARTIDA entre el dev server y los smokes (dedupe/deriva: el demo acumula rondas vencidas — el arnés de smoke-notificaciones ya es inmune). SSH al EC2: llave `C:\Users\vrubi\Downloads\lyra.pem` → copiar a `$env:TEMP` con `icacls /inheritance:r /grant:r "$env:USERNAME:R"` (re-otorgar `:F` antes de borrarla); compose del host = `docker compose -f docker-compose.prod.yml` en `/opt/watchlog/deploy`; `update.sh` NO hace `git pull`. Inspeccionar runs de GitHub sin `gh`: API REST + token del credential manager (`git credential fill`). Node ≥ 22. **L0–L4+L6 y la prueba de fuego YA están hechos — no los re-hagas; el linaje del EC2 (counter=1) NO se toca en esta sesión.**
