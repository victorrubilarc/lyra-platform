Continuamos Lyra WatchLog (`g:\Development\BitacorasInteligentes`). Ejecuta la rutina de arranque de CLAUDE.md ANTES de nada: lee CLAUDE.md y `docs/` (PROGRESS, ARCHITECTURE, DATA_MODEL, SECURITY, DECISIONS, AUTH_FLOW, BACKLOG, USER_GUIDE, FORM_GUIDE). **LECTURA OBLIGADA de este módulo (fuente de verdad, léelas COMPLETAS): `docs/LICENSING_STRATEGY.md` (el porqué — Opción C, 6 capas), `docs/LICENSING.md` (la spec técnica) y `docs/LICENSING_PROCEDURE.md` (el runbook operacional — §5 «¿quién emite?» y §2 la ceremonia de activación son EL corazón de esta sesión).** Lee además **COMPLETO** el paquete `packages/licensing` (`@lyra/licensing`, L0, 42 tests — es el CIMIENTO: `signLicense`/`verifyLicense`/`deriveFingerprint`/`evaluateLicense` + tipos con linaje) y **todo lo construido en L1** (`apps/watchlog-api/src/licensing/`: `LicenseService`, `MachineSignalsCollector`, `license-public-key.ts`, guard, `LicenseInstallation`, `tools/gen-dev-license.ts`, `scripts/license/dev-keys/`, `scripts/smoke-licencia.py`). Revisa las entradas de `docs/DECISIONS.md` del **2026-07-04** (Opción C + L0) y **2026-07-05** (L1, decisiones a–e; en especial la (d): pública EMBEBIDA, la de PROD nace en L3 con custodia). Revisa tu memoria persistente (MEMORY.md; en especial **licensing-strategy** [L0✅+L1✅, deudas L1], **channel-business-model** [banda del socio, emisión centralizada], **stack-decisions**, **product-name**, **challenge-dont-please**, **decide-well-align-to-leaders**, **new-permission-dev-gotcha**, **ci-pipeline**, **deploy-aws** [el EC2 demo que hay que licenciar]). No des nada por sentado: verifica en el código y en git.

**Antes de empezar:** confirma árbol limpio (`git rev-list --count origin/main..main` debe ser 0; L1 quedó publicado en `main` hasta el merge `b5c7c51`). Si hubiera algo sin publicar, resuélvelo antes.

**Recordatorios permanentes (challenge-dont-please + gobernanza de licenciamiento de CLAUDE.md):** estrategia **DECIDIDA = Opción C, solo software** — NO propongas dongle. Esta sesión toca EL punto más sensible de todo el sistema: **la clave PRIVADA de emisión**. Reglas nucleares que NO se negocian: la **privada JAMÁS toca el repo, una imagen Docker, un `.env`, ni un log** — vive bajo custodia (cifrada con passphrase fuera del repo o en un gestor de secretos) y solo se carga en el instante de firmar. La **pública de PROD se EMBEBE en el build (compilada), JAMÁS por variable de entorno** — una pública configurable por env sería un bypass trivial (el atacante la sustituye por la suya y firma con keygen). **Nada que rompa el air-gap** (la emisión es un proceso del EMISOR con internet; el producto en la planta sigue sin exigir salida). **Reusa `@lyra/licensing` para TODA la lógica pura** (firma/verificación): la CLI NO re-implementa criptografía. Si ves motivo de peso para desviarte de la spec/L0/L1, **objétalo con fundamento ANTES de codificar**.

== OBJETIVO DE ESTA SESIÓN (único, cerrable) — Licenciamiento L3: CLI de emisión + custodia de la clave privada + pública de PROD embebida ==
Que ITESICWS pueda **emitir licencias reales** con una CLI propia y que **la imagen construida sea por fin apta para distribuirse** (hoy NO lo es: la pública embebida es la DEV committeada — deuda registrada en L1). Alcance EXACTO:

1. **CLI de emisión `lyra-license`** (herramienta del EMISOR, **NUNCA se distribuye al cliente**; decide su ubicación en (a)): reusa `@lyra/licensing`. Comandos mínimos:
   - **`keygen`** → genera un par Ed25519 de PRODUCCIÓN, guarda la **privada CIFRADA** (passphrase) en un path FUERA del repo e imprime la **pública** lista para embeber. La privada nunca se escribe en claro.
   - **`issue`** → toma un **`solicitud.lreq`** (el archivo que L1 ya genera: `installationId` + `fingerprint`) + parámetros comerciales (`--customer`, `--channel-partner`, `--edition`, `--modules`, `--limits maxNodes/maxNamedUsers/maxInstallations`, `--expires`, `--grace-days`), arma el `LicensePayload` (huella tomada de la solicitud → node-lock real), lo **firma con la privada de PROD** (cargada de la custodia, jamás del repo) y produce `license.lic`. **Registra la emisión** en el ledger (punto 4).
   - **`inspect`** → verifica/muestra un `license.lic` (reusa `verifyLicense` + `evaluateLicense`) para QA del emisor.
   - **`ledger`/`list`** → muestra el registro de emisiones (para controlar la banda del socio, `LICENSING_PROCEDURE §3`).
   Validación de entrada fuerte (fechas ISO, edition ∈ enum, modules conocidos). Nada de secretos por argumento en claro que quede en el historial de shell (passphrase por prompt/env efímera/archivo, no por `--flag`).

2. **Custodia de la clave privada de PROD** (decide el mecanismo en (b)): la privada vive **cifrada** (passphrase → KDF fuerte + AEAD, o `age`/`sops`, o un gestor/HSM) en un path fuera del repo; la CLI la descifra en memoria solo para firmar. Documenta el procedimiento de custodia/rotación en `LICENSING_PROCEDURE.md` y `SECURITY.md §9` (docs VIVOS). **Verifica que ni la privada ni la passphrase puedan colarse al repo** (`.gitignore`, revisión del diff antes de commitear).

3. **Pública de PROD embebida — esto VUELVE VENDIBLE la imagen** (decide el mecanismo en (c)): hoy `apps/watchlog-api/src/licensing/license-public-key.ts` tiene la pública DEV. L3 debe establecer cómo el **build de PRODUCCIÓN del CI SUSTITUYE** esa constante por la pública de PROD — **sustitución en BUILD, JAMÁS por env en runtime**. Propón el mecanismo (codegen desde un GitHub Secret con la pública de prod → archivo generado gitignoreado que el runtime importa, con fallback a la DEV en dev/CI; o placeholder + reemplazo en el `Dockerfile`). El resultado: `pnpm build` local sigue usando la DEV (dev/tests verdes sin licencia real), y **solo** el pipeline de release hornea la de PROD. Deja EXPLÍCITO en la doc que a partir de aquí la imagen de release SÍ es distribuible.

4. **Registro de emisiones (ledger del emisor)** (decide formato/ubicación en (e)): dónde ITESICWS lleva la cuenta de licencias emitidas (quién, cuándo, qué `installationId`/huella/cliente/socio, con qué límites, contra qué banda). Vive en la **máquina del emisor**, **NUNCA en el producto ni en el repo** (JSON append-only o SQLite local). Es la base del control de banda del canal (`LICENSING_PROCEDURE §3/§5`). Append-only y auditable.

5. **Reconciliar el flujo DEV con la CLI:** el `pnpm license:dev` de L1 (`tools/gen-dev-license.ts`) debe seguir funcionando; idealmente pasa a ser un envoltorio delgado sobre `lyra-license issue` con el par DEV (una sola implementación de emisión, sin copiar/pegar). El par DEV committeado sigue siendo el de dev/CI.

6. **Cerrar la deuda operacional de L1 — licenciar el EC2 demo:** con la CLI ya real, **emite el `license.lic` del EC2 demo** (`lyra.watchlog.itesicws.com`): recoge su `solicitud.lreq` (por SSH del host o derivando la huella con el recolector), firma con el par **DEV** (el demo no necesita el par de PROD todavía), deja el archivo en `deploy/license/license.lic` del host y verifica que arranca `VALIDA` (mounts `/etc/machine-id` + `./license` del compose). Si prefieres no tocar el EC2 en esta sesión, déjalo como paso documentado y explícito en `BACKLOG.md`/`DEPLOYMENT.md`.

7. **Tests + smoke (obligatorios — es la cadena de confianza):** unit de la CLI (`issue` produce un `license.lic` que `verifyLicense` acepta y `evaluateLicense` da VALIDA con la huella de la solicitud; `issue` con huella ajena → el producto lo BLOQUEA; ledger append-only; `keygen` produce Ed25519 y la privada cifrada NO es legible sin passphrase). **Smoke en vivo** (`scripts/` como los demás): `keygen` → `issue` sobre un `solicitud.lreq` real → arrancar la API con ese `license.lic` → **VALIDA y opera** (reusa el arnés de `smoke-licencia.py`); y que una licencia firmada con una privada **distinta** de la pública embebida → **BLOQUEADA** (keygen del atacante rechazado, la prueba T3 del PoC pero end-to-end). Los 276 tests de la API y el CI siguen EN VERDE **sin** clave de PROD (el fallback DEV manda en dev/CI).

== NADA HARDCODEADO / NADA SUELTO ==
Rutas de la privada/passphrase/ledger por env o flag documentado (con defaults en `.env.example`/doc de la CLI), NUNCA embebidas. La pública de PROD entra SOLO por el mecanismo de build de (c). Reusa `@lyra/licensing` para firma/verificación y el `MachineSignalsCollector`/`deriveFingerprint` para cualquier huella. La CLI valida su entrada como el resto del backend.

== LO QUE L3 **NO** HACE (para no invadir sesiones vecinas) ==
- **NO** gating de módulos por entitlement en endpoints/web → **L2** (sigue pendiente; si haces L3 antes, déjalo claro en BACKLOG).
- **NO** challenge-response por USB, **NO** flujo de RENOVACIÓN ni rotación de **linaje** (counter+nonce en la renovación) → **L4** (L3 emite la licencia INICIAL atada a una huella; el ciclo de renovación con detección de clon es L4).
- **NO** bytecode/ofuscación/anti-tamper del módulo crítico → **L5**.
- **NO** UI de banner/estado ni DTO en `@lyra/contracts` → **L6** (el front NO se toca).
- **Rotación multi-clave (varias públicas embebidas + `kid` en el JWS):** por defecto **NO** en L3 salvo que en (d) se decida incluirla — implicaría tocar el L0 CONGELADO (signLicense/verifyLicense), así que se evalúa como decisión aparte, retrocompatible o diferida a su propio mini-hito.

== CONFIRMA ANTES DE CODIFICAR (espera mi visto bueno) ==
(a) **Dónde vive la CLI** (`packages/licensing-cli` como paquete del workspace vs `tools/` vs app dedicada) y cómo se invoca (`pnpm license issue …`); recuerda que NO se empaqueta en la imagen del cliente. (b) **Mecanismo de custodia de la clave privada de PROD** (archivo cifrado passphrase+KDF+AEAD propio vs `age`/`sops` vs gestor/HSM) — sin SaaS obligatorio, sin romper air-gap del producto. (c) **Mecanismo de sustitución de la pública embebida en el build de prod** (codegen desde Secret con fallback DEV vs reemplazo en Dockerfile), reafirmando build-time y jamás env runtime. (d) **Si L3 incluye rotación multi-clave (`kid`)** o se difiere — objétalo con fundamento (toca L0). (e) **Formato/ubicación del ledger** (JSON append-only vs SQLite) y si el control de banda del socio se modela aquí o solo se registra. (f) Si esta sesión **licencia el EC2 demo en vivo** o solo deja el procedimiento documentado. Si detectas una mejora sobre la spec, propónmela ahí.

== VERIFICACIÓN Y CIERRE (CLAUDE.md §Gestión de sesiones) ==
1. En verde: `pnpm typecheck && pnpm lint && pnpm build && pnpm test` + tests de la CLI + smoke en vivo de emisión→arranque (registra qué se probó y qué NO). **Deja API `:3000/api/health` y web `:5173` CORRIENDO** al cerrar.
2. Actualiza `docs/PROGRESS.md`, `docs/BACKLOG.md` (L3 hecho en §2(1); si L2 sigue pendiente, déjalo como PRÓXIMA; cierra la deuda "imagen no vendible" y la del EC2 demo si aplica), `docs/LICENSING.md`/`LICENSING_PROCEDURE.md`/`SECURITY.md §9` como docs VIVOS (§6 emisión, §5 gobernanza, custodia de clave), `docs/DECISIONS.md` (decisiones a–f) y `.env.example`/doc de la CLI. USER_GUIDE: probablemente N/A (la CLI es del emisor, no del usuario final); si documentas el proceso de activación de cara al socio, ponlo donde corresponda.
3. Commit descriptivo en rama `feat/licenciamiento-l3` + merge a `main` + **push** (incluye `.claude/settings.json` si cambió; **verifica que la privada de PROD y la passphrase NO estén en el diff**). Verifica `git rev-list --count origin/main..main` = 0.
4. Actualiza la memoria (**licensing-strategy**: L3 hecho con la CLI/custodia reales y el mecanismo de pública de PROD; deja L2 y L4 como lo que sigue; nota si la imagen ya es vendible).
5. Resumen y dime la frase de cierre con el siguiente módulo (L2 gating por entitlement, o L4 challenge-response/linaje, según lo que quede).

== ENTORNO Y GOTCHAS ==
Monorepo pnpm workspaces. `@lyra/licensing` **ya NO es source-only**: gana `dist` (espejo de `@lyra/contracts`; la API CJS no importa TS fuente — DECISIONS 2026-07-05); si lo tocas, `pnpm --filter @lyra/licensing build` o nadie ve el cambio, y agrégalo al stage `deps` del `Dockerfile.api` si sumas un paquete nuevo (como se hizo con licensing en L1). API NestJS `:3000` prefijo `/api`, web `:5173`; tests = vitest. PowerShell 5.1: NO `2>&1` con exes nativos; NO heredoc bash — commits largos con `git commit -F archivo`. Permiso nuevo ⇒ `db:seed` + Redis `FLUSHALL` o el admin demo da 403 (L3 probablemente NO agrega permiso: es una CLI del emisor, sin endpoints de usuario). El dev server corriendo puede bloquear el DLL de Prisma (`EPERM` en `prisma generate`: benigno si el cliente ya está generado, o detén el dev y reintenta). Gotcha del smoke de licencia (de L1): verifica que el puerto quede LIBRE entre arranques de la API o los checks le pegan a un proceso huérfano. Node ≥ 22.
