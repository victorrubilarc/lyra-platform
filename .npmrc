auto-install-peers=true
strict-peer-dependencies=false
engine-strict=true
# No reinstalar automáticamente antes de cada script (evita fricción con los
# build scripts nativos bloqueados por defecto en pnpm 11).
verify-deps-before-run=false
