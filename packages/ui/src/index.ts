/**
 * @lyra/ui — Design System del ecosistema Lyra.
 *
 * Fase 0: solo tokens (CSS). Importa los tokens en el entry point de la app:
 *   import "@lyra/ui/tokens.css";
 *
 * Los componentes React reutilizables (Button, Card, Chip, NodeTag, etc.)
 * se extraen del prototipo hacia este paquete en las fases siguientes.
 * Ver docs/DESIGN_SYSTEM.md.
 */
export const LYRA_UI_VERSION = "0.0.0";
